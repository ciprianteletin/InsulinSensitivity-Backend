package com.insulin.interfaces;

/**
 * Marker interface which extend both of CalculateIndex and ExcelFormula, with the role to add both functionality to a formula
 */
public interface FormulaMarker extends CalculateIndex, ExcelFormula{
}
